<h2>Editing milestone <%= @milestone.title %></h2>

<% form_for(@milestone) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form_fields', :locals => {:f => f} %>

  <hr />
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Back to index', milestones_path %>