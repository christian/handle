<div class="span-20">
  <%= render :partial => "menu" %>
  
  <h2>Productivity</h2>
  From: date Until: date
  
  <h2>Users</h2>
  <table>
    <tr><th>User</th>
        <th>Time spent today</th>
        <th>Opened tasks</th>
        <th>Closed tasks</th>
    <% for user in @users %>
        <tr class="<%= cycle('even', '') %>">
          <td><%= user.name %></td>
          <td><%= humanized_duration(user.time_spent_per_day) %></td>
          <td><%= user.opened_tasks_count %></td>
          <td></td>
        </tr>
    <% end %>
  </table>  

  Tasks completed:
  Tasks with deadline today. (add deadline per task)
  
</div>

<div class="span-4 last">
  <div style="background-color: #75CC28; color: white; padding: 7px;">
    Project &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;

  <%= select_tag 'project', options_for_select(projects_collection, @select_index.to_i), 
      {:onchange => "#{remote_function(:url  => "#{get_tasks_tasks_path}",
                                       :method => :get,
                                       :with => "'project_id='+ $(\"\#project\").val()"
                                      )
                      }"
      } %>
  </div>

  <div style="background-color: #F26522; color: white; padding: 7px;">
    Filter tasks 
  </div>
  
</div>
